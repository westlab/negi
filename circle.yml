## Customize the test machine
machine:
  timezone:
    Asia/Tokyo # Set the timezone

dependencies:
  pre:
   - sudo apt-get update; sudo apt-get install  libpcap-dev zlib1g-dev libsqlite3-dev cmake
  override:
   - cmake .; make
